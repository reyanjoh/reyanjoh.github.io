/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/reyanjoh.github.io",
    images: {
        unoptimized: true,
      },
}

module.exports = nextConfig
