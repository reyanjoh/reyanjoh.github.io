/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    distDir: "dist",
    basePath: "/reyanjoh.github.io",
    images: {
    unoptimized: true,
  },
};

export default nextConfig;
